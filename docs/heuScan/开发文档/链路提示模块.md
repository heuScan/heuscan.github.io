
# 一、模块任务描述

>链路提示模块 是整个扫描系统的" 关联发现引擎 "，它像一名侦探，在其他10个Agent收集的海量离散数据中，发现隐藏的、非直观的实体间关联关系。它不产生新的原始数据，而是在已有数据之上做二阶推理

# 二、核心功能

1. 放大情报价值 ：从N个孤立点，发现O(N²)种可能关联
2. 指导扫描方向 ：基于关联提示可以扩展扫描范围
3. 溯源支撑 ：为企业归属、资产归并提供证据链
4. 降低漏判率 ：单一维度证据不足时，多维度交叉可显著提升可信度

## 2.1 功能一：网络层关联推断

- 触发条件 ：获得≥2个IP地址列表时
- 处理逻辑 ：
1. 按/24 CIDR前缀对IP进行分组
2. 对组大小≥2的生成旁站关联提示
3. 标记证据强度为"weak"（同机房可能是多租户）
- 输出物 ： same_ip_block 类型提示
- 质量要求 ：无遗漏、无误报，IP分组算法正确

## 2.2 功能二：SSL证书关联推断

- 触发条件 ：获得证书指纹 → 域名映射表时
- 处理逻辑 ：
1. 按证书指纹聚合域名
2. 聚合大小≥2的生成强关联提示
3. 标记证据强度为"strong"（同一证书是强关联信号）
4. 自动附加建议"纳入同一目标集合"
- 输出物 ： same_certificate 类型提示
- 质量要求 ：准确识别泛域名、多域名证书覆盖范围

## 2.3 功能三：前端资产关联推断

- 触发条件 ：获得静态资源哈希 → 站点映射时
- 处理逻辑 ：
1. 计算JS/CSS文件内容哈希
2. 哈希碰撞≥2个站点时生成前端关联
3. 标记证据强度为"medium"
4. 自动附加建议"扩大前端扫描范围"
- 输出物 ： shared_js_hash 类型提示
- 质量要求 ：区分通用库（jQuery等）与定制化代码

## 2.4 功能四：DNS链路推断

- 触发条件 ：获得CNAME解析结果时
- 处理逻辑 ：
1. 反向构建：CNAME目标 → 来源域名列表
2. 来源数量≥2的生成CDN/云服务关联提示
3. 标记证据强度为"medium"
- 输出物 ： cname_chain 类型提示
- 质量要求 ：识别知名CDN域名，区分公有云与自建

## 2.5 功能五：HTTP跳转链路追踪

- 触发条件 ：获得3xx重定向链数据时
- 处理逻辑 ：
1. 还原完整跳转路径：A→B→C→D
2. 链长度≥3跳时生成隐蔽链路提示
3. 高亮展示完整跳转路径供分析
4. 标记证据强度为"medium"
- 输出物 ： redirect_chain 类型提示
- 质量要求 ：识别短链、引流、防爬虫跳转模式

## 2.6 功能六：第三方服务关联推断

- 触发条件 ：获得第三方资源引用数据时
- 处理逻辑 ：
1. 统计资源URL被引用站点数
2. ≥3个站点共享同一资源时生成"基础设施共性"提示
3. 标记证据强度为"weak"
- 输出物 ： third_party_resource 类型提示
- 质量要求 ：排除Google Analytics、Font Awesome等公共服务

## 2.7 功能七：多维度交叉验证

- 触发条件 ：已有≥2个单维度提示后
- 处理逻辑 ：
1. 对所有提示按实体进行反向索引
2. 发现出现在≥2个维度中的实体簇
3. 聚合所有维度的证据链
4. 生成"多因子交叉验证"复合提示
- 输出物 ： multi_factor_correlation 类型提示
- 质量要求 ：证据去重、高分优先、不重复不遗漏

## 2.8 功能八：标准化输出对接

- 处理逻辑 ：
  1. 为所有提示分配全局唯一ID
  2. 调用报告模块的 add_link_hint 接口
  3. 传入类型、描述、关联实体、证据数、置信度
- 输出物 ：报告模块中的链路提示章节

# 三、边界、约束与默认策略

## 3.1 输入边界

| 维度 | 有效值范围 | 超出边界时的默认行为 |
| :---: | :---: | :---: |
| IP列表分析 | 至少2个IPv4地址 | 少于2个IP不生成提示 |
| 证书关联 | 同一证书对应≥2个域名 | 单域名证书自动跳过 |
| JS资源关联 | 同一哈希对应≥2个站点 | 单站点哈希不生成关联 |
| CNAME链路 | 同一目标对应≥2个来源 | 单来源不生成聚合提示 |
| 重定向链路 | 跳转链长度≥3跳 | 两跳以内重定向不告警 |
| 第三方资源 | 同一资源被≥3个站点引用 | 少于3站不认为有共性 |
| 证据数量 | 单条提示支持1-10条 | 超过10条只取前10条强证据 |
---

# 3.2 输出边界

- 单分析批次最大生成 64条 推断性提示
- 单条提示关联实体数 2-32个
- 证据分区间 [0.0, 1.0] ，保留3位小数
- 置信度分级：very_low / low / medium / high
- is_inferred字段 恒为True （本模块所有结论均为推断）

# 3.3 默认策略

- 保守推断原则 ：宁漏勿错，所有结论标记为推断性，建议人工复核
- 无状态计算 ：所有分析基于当前输入快照，不维护历史状态
- 证据降级 ：跨源关联证据强度自动降一级处理
- 去重机制 ：同一实体组合不重复生成同类型提示
- 超时保护 ：单批次分析最长执行时间 5秒 ，超出截断返回已生成结果

# 四、职责拆解

## 4.1 核心职责

- 多维度关联推断引擎 ：IP网段、SSL证书、JS哈希、DNS解析、重定向链、第三方资源等维度的关联识别
- 证据链加权评分 ：基于证据类型、强度、数量的置信度量化计算
- 多维度交叉验证 ：跨维度证据聚合，生成复合关联结论
- 推断结论标准化输出 ：统一格式、统一置信度分级、统一建议话术
- 图谱关系传播 ：基于种子实体的间接关联发现

## 4.2 非职责

- 不做确定性结论 ：所有输出均为"提示"而非"事实"，不替代人工研判
- 不维护实体图谱 ：不持久化实体和关系数据，只做单次批处理
- 不进行原始数据采集 ：不主动扫描、不爬取、不做DNS/HTTP探测
- 不实现去重逻辑 ：输入数据去重由上游调用方保证
- 不与外部系统交互 ：纯内存计算，无IO、无网络调用

## 4.3 与其他模块的职责边界

| 相邻模块 | 该模块做什么 | 该模块做什么 | 交互约定 |
| :---: | :---: | :---: | :---: |
| 图谱聚合Agent | 维护实体-关系持久化图谱、实体归并 | 基于图谱快照做关联推断 | 本模块输入图谱查询结果，输出可溯源的推断边 |
| 基础发现Agent | 收集IP、端口、证书、DNS等原始数据 | 发现原始数据中的隐含关联模式 | 接收结构化的探测结果，生成关联提示 |
| JS解析Agent | 提取静态资源哈希、外联域名、API路径 | 跨站点资源相似性比对 | 接收JS哈希聚合结果，生成前端资产关联 |
| 报告输出模块 | 多源数据聚合、多格式导出、报告生成 | 生成标准化链路提示结构 | 调用 add_to_scan_report() 方法注入结果 |
| 复核Agent | 人工确认/驳回/标注冲突 | 提供证据链支撑复核决策 | 所有结论置信度不超过medium，强制进入复核池 |
---

# 五、代码处理流程

```
原始输入数据
    ↓
[ 数据预处理层 ]
    ├─ 格式校验与类型转换
    ├─ 空值与异常值过滤
    └─ 输入维度归一化
    ↓
[ 单维度推断引擎 ]
    ├─ IP网段聚类 → CIDR前缀分组算法
    ├─ 证书分组 → 证书指纹聚合
    ├─ JS哈希关联 → 内容哈希碰撞检测
    ├─ CNAME链路 → 反向域名聚合
    ├─ 重定向链路 → 跳转链长度检测
    └─ 第三方资源 → 资源引用频次统计
    ↓
[ 多维度交叉验证层 ]
    ├─ 按实体维度收集所有单维度提示
    ├─ 出现频次≥2的实体标记为强关联
    ├─ 聚合跨维度证据链
    └─ 生成多因子关联提示
    ↓
[ 加权评分计算层 ]
    ├─ 提示类型基础分
    ├─ 单条证据强度加权
    ├─ 证据数量奖励分
    └─ 置信度等级映射
    ↓
[ 输出标准化层 ]
    ├─ 生成全局唯一HintID
    ├─ 推断性标记设置
    ├─ 处置建议自动生成
    └─ 按证据分降序排序
    ↓
链路提示列表 → 对接报告模块
```

# 六、关键算法分析

## 6.1 加权置信度评分算法

### 6.1.1 数学公式
```
最终得分 = 基础分 * 0.6 + 证据平均分 * 0.3 + 数量奖励

其中：
- 基础分 = TYPE_BASE_SCORE[提示类型]  范围[0.2, 0.8]
- 证据平均分 = Σ(证据强度权重 * 自定义权重) / N
- 数量奖励 = min(0.2, N * 0.03)  每增加1条证据+0.03，上限0.2
```

### 6.1.2 权重配置

| 证据强度 | 权重值 | 说明 |
| :---: | :---: | :---: |
| very_strong | 1.0 | 确定性事实，如证书哈希精确匹配 |
| strong | 0.75 | 强统计学关联 |
| medium | 0.5 | 中等可信度关联 |
| weak | 0.25 | 弱相关性，需谨慎对待 |
| very_weak | 0.1 | 仅供参考的巧合性相似 |
---

### 6.1.3 置信度映射阈值

| 最终得分区间 | 置信度等级 |
| :---: | :---: |
| [0.8, 1.0] |medium |
| [0.5, 0.8) | low |
| [0.0, 0.5) | very_low |
---
**关键设计决策 ：**
本模块最高置信度为medium，不输出high等级，强制所有推断经过人工复核。

## 6.2 多维度交叉验证算法

### 6.2.1 伪代码

```python
def cross_validation(all_hints):
    # 第一步：构建反向索引
    entity_to_hints = defaultdict(list)
    for hint in all_hints:
        for entity in hint.entities:
            entity_to_hints[entity.value].append(hint)
    
    # 第二步：筛选出现在≥2个提示中的节点
    for entity_value, related_hints in entity_to_hints.items():
        if len(related_hints) >= 2:
            # 该实体存在多维度证据
            all_linked_entities = collect_all_entities(related_hints)
            all_evidences = collect_all_evidence(related_hints)
            
            # 第三步：生成复合提示
            yield create_multifactor_hint(
                central_entity = entity_value,
                linked_entities = all_linked_entities - {entity_value},
                evidence = all_evidences[:10]
            )
```

### 6.2.2 算法价值

- 发现"隐藏的强关联" ：单一维度证据弱但多维度同时出现时，可信度显著提升
- 例 ：A和B既共享证书，又共享JS哈希 → 关联可信度远高于单一维度

## 6.3 IP网段无监督聚类

```python
cidr_prefix = 24  # 默认/24网段
for ip in ip_list:
    octets = ip.split('.')
    network_prefix = '.'.join(octets[:3])  # 取前3个八位组
    groups[network_prefix].append(ip)

for prefix, ips in groups.items():
    if len(ips) >= 2:
        # 生成同网段关联提示
        yield same_subnet_hint(ips)
```

# 七、测试计划与验收标准

## 7.1 测试计划

| 测试类型 | 测试场景 | 验收要点 |
| :---: | :---: | :---: |
| 单元测试 | IP网段分析 | 2个及以上同/24IP生成提示；孤立IP不生成 |
|  | 证书共享推断 | 同证书≥2域名生成提示；单域名不生成 |
|  | JS哈希关联 | 同哈希≥2站点生成提示；唯一哈希不触发 |
|  | CNAME链路推断 | 同一目标≥2来源生成聚合提示 |
|  | 重定向链路检测 | ≥3跳生成提示；2跳不告警 |
|  | 第三方资源关联 | ≥3站点共享资源生成提示 |
|  | 手动添加提示 | 自定义实体和证据正确入库 |
| 集成测试 | 多维度交叉验证 | 同一实体跨≥2维度时生成复合提示 |
|  | 证据链加权评分 | 强证据对应高分；证据越多分越高 |
|  | 置信度分级 | 分数区间与等级映射正确 |
|  | is_inferred标记 | 所有提示均标记为推断性 |
|  | JSON导出 | 所有字段序列化完整无丢 |
|  | 报告对接 |可注入ScanReport且格式兼容 |
| 边界测试 | 空输入 | 不崩溃、不报错、返回空列表 |
|  | 单元素输入 | 静默跳过，不生成提示 |
|  | 大量数据 | 性能可接受，无内存泄漏 |
|  | 重复数据 | 不重复生成相同提示 |
| 异常测试 | 非法IP格式 | 跳过坏数据继续处理 |
|  | 畸形证书哈希 | 字符串匹配不受格式影响 |
|  | 超量证据 | 截断至前10条 |
---

## 7.2 验收标准

- 功能完整性 ：12种关联类型全部实现可调用
- 推断标记 ：所有输出 is_inferred=True ，无一例外
- 置信度上限 ：最高置信度为medium，不输出high等级
- 性能指标 ：1000条输入处理时间 < 100ms
- 可追溯性 ：每条提示携带证据来源、类型、完整描述
- 兼容性 ： add_to_scan_report() 接口与报告模块无缝对接
- 幂等性 ：相同输入重复调用生成结果一致
- 健壮性 ：异常输入不崩溃，优雅降级

# 八、测试用例

## 8.1 测试用例执行报告

| 测试分类 | 用例数 | 通过 | 通过率 |
| :---: | :---: | :---: | :---: |
| 单元测试 | 9 | 9 | 100% |
| 集成测试 | 6 | 6 | 100% |
| 边界测试 | 5 | 5 | 100% |
| 异常测试 | 3 | 3 | 100% |
| 总计 | 23 | 23 | 100% |
---

## 8.2 各测试分类覆盖要点

### 单元测试（9个）

**覆盖7种核心推断类型 + 手动添加提示：**

1. 同网段IP关联（含孤立IP验证）
2. SSL证书共享关联
3. JS静态资源哈希关联
4. CNAME链路推断
5. HTTP重定向链路（含2跳不告警验证）
6. 第三方资源关联
7. 手动添加推断性提示 

### 集成测试（6个）

**验证模块核心设计约束：**

1. 多维度交叉验证 - 跨维度证据聚合生成复合提示
2. 加权评分等级 - 证据强度与置信度映射正确
3. 置信度天花板 - 即使满分证据也 不输出high等级
4. 推断性标记 - 所有提示100%标记is_inferred=True
5. 统计与JSON导出格式正确性
6. 数据完整性验证 

### 边界测试（5个）

**极端输入场景验证：**

1. 空列表/空字典输入
2. 单元素输入（不生成提示）
3. 按证据分过滤
4. 按置信度等级过滤
5. len()接口与对象长度获取 

### 异常测试（3个）

**异常容错验证：**

1. 非法IP格式输入不崩溃
2. 超量证据处理不溢出
3. 排序机制正确性

## 8.3 核心示例代码速览

### 8.3.1 最基础使用 - 网段旁站发现

```python
from Chain_Hint_Agent import ChainHintAgent

agent = ChainHintAgent("项目A")

ip_list = ["47.100.123.10", "47.100.123.11", "47.100.123.12", "47.100.123.20"]
hints = agent.analyze_ip_ranges(ip_list)

for hint in hints:
    print(f"[发现] {hint.description}")
    print(f"   涉及IP: {', '.join([e.entity_value for e in hint.entities])}")
    print(f"   处置建议: {hint.suggestion}")
```

### 8.3.2 SSL证书关联取证

```python
cert_to_domains = {
    "sha256:5a7c9e...8f2d": [
        "www.antgroup.com",
        "antgroup.com",
        "tech.antgroup.com",
    ]
}

hints = agent.analyze_shared_certificates(cert_to_domains)
```
**输出:**
 4个域名共享同一张SSL证书，建议纳入同一目标集合

### 8.3.3 多维度交叉验证

```python
# 第一步：各个维度单独分析
agent.analyze_shared_certificates({"cert_corp": ["www.corp.com", "api.corp.com"]})
agent.analyze_shared_js_resources({"hash_main": ["www.corp.com", "api.corp.com"]})
agent.analyze_cname_chains({"cdn.corp.net": ["www.corp.com", "static.corp.com"]})

# 第二步：✨ 多维度交叉验证
cross_hints = agent.run_cross_validation()
```
**输出效果 ：**
 [交叉验证] 多维度交叉验证: www.corp.com 与 5 个实体存在 3 种关联证据

### 8.3.4 隐蔽重定向链路追踪

```python
redirect_chains = {
    "http://short.link/abc123": [
        "https://landing.page/verify",
        "https://middle.proxy/check",
        "https://final.destination/page",
    ],
}

hints = agent.analyze_redirect_chains(redirect_chains)
```

**输出效果 ：**
```
[重定向] 发现长度为 4 跳的重定向链路
   完整跳转路径:
        http://short.link/abc123
     -> https://landing.page/verify
     -> https://middle.proxy/check
     -> https://final.destination/page
```

### 8.3.5 完整的扫描后分析工作流

```python
agent = ChainHintAgent("完整扫描项目")

agent.analyze_ip_ranges(["10.0.1.5", "10.0.1.6", "10.0.1.7"])
agent.analyze_shared_certificates({"cert_xyz": ["site1.com", "site2.com"]})
agent.analyze_shared_js_resources({"hash_abc": ["site1.com", "site2.com", "site3.com"]})
agent.analyze_cname_chains({"cdn.cloud.io": ["a.com", "b.com", "c.com"]})

# 关键一步：交叉验证
agent.run_cross_validation()

# 导出结果
stats = agent.get_statistics()
print(f"总计发现 {stats['total_hints']} 条推断性链路提示")
agent.export_json("output/hints.json")
```

# 九、模块关键调用接口

| 方法 | 输入 | 输出 | 场景 |
| :---: | :---: | :---: | :---: |
| analyze_ip_ranges() | IP列表 | 同网段关联 | 旁站扫描 |
| analyze_shared_certificates() | 证书→域名映射 | 证书关联 | 企业资产归集 |
| analyze_shared_js_resources() | 哈希→站点映射 | 前端指纹关联 | 同源站点识别 |
| analyze_cname_chains() | CNAME→来源映射 | DNS链路 | CDN/云服务识别 |
| analyze_redirect_chains() | 重定向图 | 跳转链路 | 隐蔽通道发现 |
| analyze_third_party_resources() | 资源→站点映射 | 基础设施关联 | 团伙识别 |
| run_cross_validation() | 无(基于已有提示) | 复合关联 | 多因子强关联 |
---